# Superseded diagnostic material

These files are retained only to explain rejected intermediate geometry or routes. They are not the current design, fabrication inventory or acceptance evidence.

Current geometry is `../step/SMALL_ATC_DOCK_CANDIDATE.step`; current fit evidence is `../fit-report.json`, and current route evidence is `../carrier-route-report.json`. The current manufactured STEP/DXF inventory is checked by `../export-inventory.json`.

- The old 21 mm pad leaves an unsupported gap and is not used.
- The old common-pattern receiver was replaced by separate left/right parts with shifted outer M5 columns.
- The earlier cap-head extraction hits the continuous Y rails; the current configuration uses lower button-head screws and a revised locator/wing height.
- Earlier fit reports bind earlier source hashes and must not substitute for current evidence.

# Private GitHub repository

- Repository: https://github.com/DeadlySIn777/plasma-router-stand
- Visibility requested and verified: **PRIVATE**
- Git checkout: `C:/Users/Gluis/OneDrive/Documents/GitHub/plasma-router-stand`
- Initial branch: `codex/initial-import`

The repository contains a reviewed file snapshot of this project, not the parent multi-project workspace. Source files, CAD exports, documents, the BOM, controller source and audit reports are included. Local runtimes, dependency caches, raw web captures and duplicate archives are excluded; the exact imported file hashes are in `REPOSITORY-SNAPSHOT.json` in the Git checkout.

This original working directory and the Git checkout are separate. Later edits here are not uploaded automatically. Transfer reviewed changes to the Git checkout and commit/push them explicitly. Do not publish the parent workspace to this repository.

The project remains an unreleased engineering design with open re-audit findings. Repository publication is not design approval or completion of the re-audit.
